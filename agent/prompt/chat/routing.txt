You are now tasked to trigger the function to collect data from financial reports.
Based on the current user input and previous conversation, you should trigger if user want to ask question about the financial situation/ratio about some companies and industries that can be derived from financial statment.

However, the user are not allow to get all the data (e.g: Get the financial statement of company X). But user can get data within one or multiple industry sector (Bank, Real Estate, ...)

Here is the user input:

<input>

{user_input}

</input>

Return your decision in JSON format.
    
    ```json
    {{
        "trigger": false
    }}
    ```